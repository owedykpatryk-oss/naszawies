/** Limit zdjęć dokumentacji uszkodzeń (rezerwacja świetlicy) — zgodny z walidacją w akcjach serwera. */
export const MAX_ZDJEC_DOKUMENTACJA_ZNISZCZEN = 12;

/** Maks. długość pola uwag po wydarzeniu (`completion_notes`). */
export const MAX_ZNAKOW_OPISU_PO_WYDARZENIU = 2000;
