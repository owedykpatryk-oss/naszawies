-- Wpis w `supabase_migrations.schema_migrations` istnieje na zdalnej bazie bez odpowiadającego pliku w repo.
-- Ten plik wyrównuje historię dla Supabase CLI (`db push`). Nie wykonuj tu DDL — zmiany były już na serwerze.
SELECT 1;
